@model  IEnumerable<PhoneStoreWeb.Models.tb_ProductCategory>
<ul class="sidebar_categories">
    <ul class="sidebar_categories">
        @foreach (var i in Model)
        {
            @Html.ActionLink(i.Title, "getProductbyMeta", "Category", new { @meta = i.Meta }, null)
            <br>

        }
</ul>

</ul>

